/**
 * 型定義のエクスポートポイント
 * 
 * このファイルから全ての型定義をインポートできます。
 * 例: import { GachaCard, CardResult } from '@/types';
 */

export type {
    GachaStatus,
    GachaCategory,
    CardRarity,
    CardResult,
    GachaCard,
    GachaCardProps,
    GachaResultModalProps,
} from './gacha';
